api:
  build:
    deploy: [ './app.py', './data.json' ]
    addToRunPrepare: [ 'requirements.txt' ]
  run:
    prepare:
      - python3 -m pip install -r requirements.txt
    start: waitress-serve --call app:create_app
